// Home page
// The default page when first visiting the site.
// Includes a hero image and some text

import './Home.css';

function Home() {
    return (
      <div className="home">
        
      </div>
    );
  }
  
  export default Home;